Pytanie
1) czy dodawac recznie kategorie
#TO FIX:
- po usunieciu none z filtra i powtornym wcisnieciu pokaz unikalne pojawia sie none podwujnie




TO DO:
24) przy wczytywaniu pliku z excela zeby nie pobieral pustych rekordow
24) Columna count i liczba.emisji ma byc jedna
25) dodac formularz do usuwania duplikatow
26) dodac formularz do automatycznego adpalania filtrow recznych.
19) dodac kasowanie istniejacego raportu
14) zaznaczenie rodzica powinno zaznaczać wszystkie dziecki

<< ##################################################################### >>
23) przy przyklejaniu w techedzu ma wkleja sie TV w medium
17)  def getCheckedItemsTable(self): DO poprawy kolumny nie moga byc na sztywno
22) przenoszenie raportów do excela
21) dodac automat do wczytywania filtrów
18) show unice value in table
20) przypisywanie działa ale bardzo wolno:
    a) zrobić funkcje która zczytuje wszystkie dane
    b) kasuje wszystkie wiersze
    c) podmienia kolumne w danych na kolumne z przypisem
    e) wczytuje wszystkie dane
16) dodać funkcje wczytującą wszytkie categorie przy inicie filechose
15) jak powinno działac dodawanie/usuwanie kategori kategori:
        1) najpier wybierz wszystkie kategorie dla danego filtra z bazy
        2) usuń wybrane
        3) przejdz przez drzewo, dla każdego rodzica dodaj nowa kategorię
                3a) jeżeli rodzic ma dzieci zapisz utworz liste zapisz do bazy
13) dodać usuwanie elementow >> sa przenoszone spowrotem na listę
11) Puste chyba nie mogą być puste bo się nie pojawiaja
10) zabezpieczyć się przed dodaniem nie inta lub flota do bazy dla zdefiniowanej kolumny
9) Zastanowic sie czy da sie zautomatyzowac dodawanie wierszy do bazy podczas zapisu formularza
8) Dadoc wszystkie kolumny z pliku do katalogu
7) jak nie zawijac tekstu w akrkusza
7b) Jak zmienic kolor na niebieski

7a) Jak dopasowac wielkosc kolumn do wielkosci szerokosci tekstu
12) dodac checkbox dla dzieci
6) Poprawic wczytywanie danych w filechoser po ponownym otwarciu
1) przesyłamy do klasy main scieżki do pliku excela
2) możemy zrobić odzielną klasę do machowania plików (w trakcie)
3) sprawdzić działanie clasy excela
4) dodac dane z adxperta
5) dodac zapis do bazy


IDEA
# Przekazujemy do naglowka juz wybrane wiersze

JAK laczyc stringi
tex_2.replace(tex_1,"")